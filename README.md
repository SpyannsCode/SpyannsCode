Random Unity scripts for random things:

 - Moving Windows Script (MoveWindows.cs):
   Does about what you'd expect. Allow the user to move windows, while also setting them to be seen on top. This has only been tested for UI elements.
